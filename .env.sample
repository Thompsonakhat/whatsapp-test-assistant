# CookMyBots webhook secret for managed WhatsApp transport.
# CookMyBots injects this during deployment. Do not share it.
CMB_WHATSAPP_WEBHOOK_SECRET=

# HTTP server port. Defaults to 3000 when missing.
PORT=3000
